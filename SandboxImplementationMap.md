I will implement the sandbox by treating it as the data foundation of my project. My goal in Phase 1 is to build a realistic but safe test environment that looks like a real traffic site, without connecting to any real traffic control system. I will build it for one site only, and in my case that site is Wadi Saqra Signals. Since I do not have a real public live camera for that exact location, I will use a representative traffic video from another public webcam or a recorded street video only for simulation and testing. This matches the idea of the hackathon, where Phase 1 is about building dummy datasets and simulated feeds that imitate live traffic operations for the first site.

I will start with the folder structure and build each part one by one. Inside traffic_sandbox/, I will use live_feed/ for one video that behaves like a live source, even if I begin with a normal MP4 played in a loop. I will use historical_video/ for short clips such as normal traffic, peak traffic, congestion, and abnormal stopping. I will use detector_logs/ to store vehicle count data in 15-minute intervals, signal_logs/ for traffic signal phase changes, metadata/ for the site layout, camera information, lanes, and queue zones, annotations/ for labeled traffic events, and docs/ for the data dictionary and methodology note. This structure matches both your planned sandbox organization and the hackathon’s required Phase 1 deliverables.

I will implement it in a simple order. First, I will define the site in the metadata/ folder by creating files such as intersection_metadata.json, lane_map.json, and zones.json. In these files, I will describe the site name, camera ID, road approaches, lane labels, stop lines, and queue monitoring zones, because the hackathon expects the metadata pack to describe camera location, field of view, lane configuration, approach labels, and monitoring zones. Then I will place my visual source in live_feed/ and organize useful clips in historical_video/. After that, I will create detector_counts.csv with realistic traffic counts and signal_timing_log.csv with values like timestamp, intersection ID, phase number, and signal state such as GREEN_ON, YELLOW_ON, and RED_ON. This is important because the sandbox must include traffic counts and signal timing logs for later forecasting and recommendation logic.

Next, I will build the annotation layer in annotations/. I will create files like incidents.csv and event_windows.csv and label selected events such as congestion, abnormal stopping, unusual movement, and queue spillback. I do not need to label everything from the beginning. I only need enough labeled examples to support testing and validation. The hackathon expects this layer to support training and evaluation of downstream AI modules, and it specifically mentions incident labels, congestion-event labels, abnormal stopping labels, unexpected trajectory labels, queue spillback markers, and selected event windows.

After that, I will finish the sandbox by writing the documentation in docs/. I will create a data_dictionary.md to explain what each file and column means, and I will create a methodology_note.md to explain how I got or simulated the video, how I built the counts and logs, and what assumptions I used. This is not extra work only for presentation; it is part of the required Phase 1 deliverables. At the end, I should have a live-like video source, historical clips, detector data, signal logs, metadata, annotations, and documentation, and this full package will become the base for the next phases of AI detection, forecasting, dashboard work, and integration.